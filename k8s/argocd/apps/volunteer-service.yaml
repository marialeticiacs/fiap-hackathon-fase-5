apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: volunteer-service
  namespace: argocd
  labels:
    project: solidarytech
    environment: production
spec:
  project: default
  source:
    repoURL: https://github.com/marialeticiacs/fiap-hackathon-fase-5
    targetRevision: main
    path: k8s/base/volunteer-service
  destination:
    server: https://kubernetes.default.svc
    namespace: solidarytech
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true